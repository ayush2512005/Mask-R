{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm turbo build --filter=@redact/web...",
  "outputDirectory": "packages/web/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
