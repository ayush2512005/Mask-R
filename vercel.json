{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @redact/web build",
  "outputDirectory": "packages/web/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
